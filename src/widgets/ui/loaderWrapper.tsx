'use client';

import { Loader, CircleLoader } from '@/src/widgets/ui/loader';

export function LoaderWrapper() {
  return <Loader />;
}

export function CircleLoaderWrapper() {
  return <CircleLoader />;
}
