export interface ChartProps {
  rank: number;
  nickname: string;
  profileUrl: string;
  elapsedTime: string;
  solvedCount: number;
}

export interface MyChartProps {
  rank: number;
  elapsedTime: string;
  solvedCount: number;
}